import { ScrollView, Text } from "react-native";
import React from "react";

export default function HomeScreen() {
  return (
    <>
      <ScrollView className="bg-white">
        <Text className="text-blue-600 font-bold text-4xl">Home Screen</Text>
      </ScrollView>
    </>
  );
}
